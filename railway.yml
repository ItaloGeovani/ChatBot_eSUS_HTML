version: 1
frontend:
  - name: app
    commands:
      - pip install -r requirements.txt
    plugins:
      - python

plugins:
  - name: python
    buildCommand: pip install -r requirements.txt
    runCommand: python app.py
